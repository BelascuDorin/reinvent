package com.reinvent.identity.internal;

/** Keys for values the identity module stores on the HTTP session. */
final class SessionAttributes {

	/** The signed-in User's id. */
	static final String USER_ID = "reinvent.userId";

	private SessionAttributes() {
	}
}
